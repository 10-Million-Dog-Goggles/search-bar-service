config:
  target: 'http://localhost:3001'
  tls:
    rejectUnauthorized: false
  phases:
    - duration: 2
      arrivalRate: 800
  defaults:
    headers:
      Content-Type: "application/json"
scenarios:
  - flow:
    - get:
        url: "/api/search/"
        qs:
          term: "boat"