config:
  target: 'http://localhost:3001'
  tls:
    rejectUnauthorized: false
  phases:
    - duration: 7
      arrivalRate: 400
  defaults:
    headers:
      Content-Type: "application/json"
scenarios:
  - flow:
    - get:
        url: "/api/search/"
        qs:
          term: "tabu"